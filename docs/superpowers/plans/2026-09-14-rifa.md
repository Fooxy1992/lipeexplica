# Rifa — Kimono Completo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar sistema completo de rifa ao site: venda de bilhetes (quantidade livre com precificação por faixas), pagamento via Stripe (cartão + PIX), sorteio automático 5 dias após todos os 200 bilhetes serem vendidos.

**Architecture:** Domain entities → ports → use cases → Supabase repositories, seguindo o padrão existente do projeto. Reserva atômica de bilhetes via Supabase RPC (PostgreSQL function) para evitar overselling. Sorteio via Vercel Cron Job. Stripe Checkout Sessions criadas sob demanda com `price_data` (sem price ID fixo, pois o valor varia por quantidade).

**Tech Stack:** Next.js 15 (App Router), Supabase, Stripe (cartão + PIX automático em BRL), Tailwind CSS, TypeScript, Vercel Cron.

**Spec:** `docs/superpowers/specs/2026-09-14-rifa-design.md`

## Global Constraints

- TypeScript estrito — sem `any`
- Moeda: `brl`, valores sempre em centavos (`amountCents`)
- Idioma do usuário: português brasileiro em todas as mensagens de erro e UI
- Padrão de erro: `new DomainError(code, message)` de `@/core/domain/errors/domain-error`
- Padrão de repositório: classe implementando interface Port, recebe `SupabaseClient` no constructor
- Padrão de logger: `logger.child({ scope: 'nome' })` + `log.info/warn/error('evento', { dados })`
- Padrão de mapper: funções puras `toX(row: XRow): X` em `src/infrastructure/repositories/mappers.ts`
- Admin usa `adminContainer()` (service-role, bypass RLS); user APIs usam `userScopedContainer()`
- Precificação por faixa: 1-2 bilhetes = R$25/un, 3-4 = R$20/un, 5-9 = R$18/un, 10+ = R$15/un
- Total de bilhetes: 200; reserva expira em 30 min se não paga
- Sorteio: 5 dias após vender o último bilhete, via cron diária 10:00 BRT

---

## File Map

### Novos arquivos
- `supabase/migrations/0020_rifa.sql` — tabelas + RPC de reserva atômica
- `src/core/domain/entities/raffle.ts`
- `src/core/domain/entities/raffle-ticket.ts`
- `src/core/domain/entities/raffle-purchase.ts`
- `src/core/application/ports/raffle-repository.ts`
- `src/core/application/ports/raffle-ticket-repository.ts`
- `src/core/application/ports/raffle-purchase-repository.ts`
- `src/core/application/use-cases/calculate-raffle-price.ts` — função pura, sem repositório
- `src/core/application/use-cases/purchase-raffle-tickets.ts`
- `src/core/application/use-cases/confirm-raffle-payment.ts`
- `src/core/application/use-cases/release-expired-reservations.ts`
- `src/core/application/use-cases/draw-raffle-winner.ts`
- `src/infrastructure/repositories/supabase-raffle-repository.ts`
- `src/infrastructure/repositories/supabase-raffle-ticket-repository.ts`
- `src/infrastructure/repositories/supabase-raffle-purchase-repository.ts`
- `src/app/api/raffle/checkout/route.ts`
- `src/app/api/raffle/[id]/status/route.ts`
- `src/app/api/raffle/confirmacao/[token]/route.ts`
- `src/app/api/cron/raffle-draw/route.ts`
- `src/app/api/cron/raffle-cleanup/route.ts`
- `src/app/rifa/page.tsx`
- `src/app/rifa/confirmacao/[token]/page.tsx`
- `src/app/admin/rifa/page.tsx`

### Arquivos modificados
- `src/infrastructure/supabase/database.types.ts` — adicionar Row types
- `src/infrastructure/repositories/mappers.ts` — adicionar mappers de rifa
- `src/infrastructure/di/container.ts` — registrar repos + use cases
- `src/app/api/stripe/webhook/route.ts` — tratar `checkout.session.completed` de rifa
- `src/components/admin/sidebar-nav.tsx` — adicionar item "Rifa"
- `vercel.json` — adicionar 2 cron jobs

---

## Task 1: Migration SQL — Tabelas e RPC

**Files:**
- Create: `supabase/migrations/0020_rifa.sql`

**Interfaces:**
- Produces: tabelas `raffles`, `raffle_tickets`, `raffle_purchases`; RPC `reserve_raffle_tickets`

- [ ] **Step 1: Criar migration**

```sql
-- supabase/migrations/0020_rifa.sql

-- ─── raffles ───────────────────────────────────────────────────────────────────
create table public.raffles (
  id                uuid        primary key default gen_random_uuid(),
  title             text        not null,
  prize_name        text        not null,
  prize_image_url   text,
  total_tickets     int         not null default 200,
  status            text        not null default 'active'
                    check (status in ('active', 'sold_out', 'drawing', 'completed')),
  draw_date         timestamptz,
  winner_ticket_id  uuid,
  created_at        timestamptz not null default now()
);

-- ─── raffle_tickets ────────────────────────────────────────────────────────────
create table public.raffle_tickets (
  id              uuid        primary key default gen_random_uuid(),
  raffle_id       uuid        not null references public.raffles (id) on delete cascade,
  ticket_number   int         not null,
  status          text        not null default 'available'
                  check (status in ('available', 'reserved', 'paid')),
  reserved_until  timestamptz,
  purchase_id     uuid,
  created_at      timestamptz not null default now(),
  unique (raffle_id, ticket_number)
);

create index idx_raffle_tickets_raffle_status on public.raffle_tickets (raffle_id, status);
create index idx_raffle_tickets_reserved_until on public.raffle_tickets (reserved_until)
  where status = 'reserved';

-- ─── raffle_purchases ──────────────────────────────────────────────────────────
create table public.raffle_purchases (
  id                  uuid        primary key default gen_random_uuid(),
  raffle_id           uuid        not null references public.raffles (id) on delete cascade,
  stripe_session_id   text        unique,
  ticket_quantity     int         not null check (ticket_quantity >= 1),
  amount_cents        int         not null check (amount_cents > 0),
  status              text        not null default 'pending'
                      check (status in ('pending', 'paid', 'failed', 'expired')),
  buyer_name          text        not null,
  buyer_email         text        not null,
  buyer_phone         text,
  confirmation_token  text        unique default encode(gen_random_bytes(16), 'hex'),
  created_at          timestamptz not null default now()
);

create index idx_raffle_purchases_raffle_status on public.raffle_purchases (raffle_id, status);
create index idx_raffle_purchases_session on public.raffle_purchases (stripe_session_id);
create index idx_raffle_purchases_token on public.raffle_purchases (confirmation_token);

-- FK de raffle_tickets.purchase_id → raffle_purchases.id (deferred pois purchase é criado primeiro)
alter table public.raffle_tickets
  add constraint fk_raffle_tickets_purchase
  foreign key (purchase_id) references public.raffle_purchases (id) on delete set null;

-- FK de raffles.winner_ticket_id → raffle_tickets.id
alter table public.raffles
  add constraint fk_raffles_winner_ticket
  foreign key (winner_ticket_id) references public.raffle_tickets (id) on delete set null;

-- ─── RPC: reserva atômica ──────────────────────────────────────────────────────
create or replace function public.reserve_raffle_tickets(
  p_raffle_id  uuid,
  p_quantity   int,
  p_purchase_id uuid
) returns table(id uuid, ticket_number int) as $$
declare
  v_available int;
begin
  -- Conta bilhetes realmente disponíveis (exclui reservas expiradas tratando como available)
  select count(*) into v_available
  from public.raffle_tickets
  where raffle_id = p_raffle_id
    and (
      status = 'available'
      or (status = 'reserved' and reserved_until < now())
    );

  if v_available < p_quantity then
    raise exception 'INSUFFICIENT_TICKETS: % disponíveis, solicitado %', v_available, p_quantity;
  end if;

  -- Reserva atomicamente (SKIP LOCKED evita deadlock sob concorrência)
  return query
  update public.raffle_tickets t
  set
    status         = 'reserved',
    reserved_until = now() + interval '30 minutes',
    purchase_id    = p_purchase_id
  where t.id in (
    select inner_t.id
    from public.raffle_tickets inner_t
    where inner_t.raffle_id = p_raffle_id
      and (
        inner_t.status = 'available'
        or (inner_t.status = 'reserved' and inner_t.reserved_until < now())
      )
    order by random()
    limit p_quantity
    for update skip locked
  )
  returning t.id, t.ticket_number;
end;
$$ language plpgsql security definer;

-- Só o service role pode chamar esta RPC
revoke execute on function public.reserve_raffle_tickets(uuid, int, uuid) from public, anon, authenticated;
grant  execute on function public.reserve_raffle_tickets(uuid, int, uuid) to service_role;
```

- [ ] **Step 2: Aplicar no Supabase**

```bash
# Opção A: Supabase CLI linkado
npx supabase db push

# Opção B: colar SQL diretamente no Supabase Studio → SQL Editor
```

- [ ] **Step 3: Seed — criar a rifa e gerar os 200 bilhetes**

Execute no SQL Editor do Supabase Studio (substitua a URL da imagem):

```sql
do $$
declare
  v_raffle_id uuid;
begin
  insert into public.raffles (title, prize_name, prize_image_url, total_tickets)
  values (
    'Rifa Kimono Completo',
    'Kimono Completo',
    'https://SEU-DOMINIO/imagem-kimono.jpg',
    200
  )
  returning id into v_raffle_id;

  insert into public.raffle_tickets (raffle_id, ticket_number)
  select v_raffle_id, generate_series(1, 200);
end $$;
```

- [ ] **Step 4: Confirmar no Studio**

Verificar: `raffles` tem 1 linha com `status = 'active'`; `raffle_tickets` tem 200 linhas com `status = 'available'`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/0020_rifa.sql
git commit -m "feat(rifa): add raffle tables and atomic reservation RPC"
```

---

## Task 2: Domain Entities + Row Types

**Files:**
- Create: `src/core/domain/entities/raffle.ts`
- Create: `src/core/domain/entities/raffle-ticket.ts`
- Create: `src/core/domain/entities/raffle-purchase.ts`
- Modify: `src/infrastructure/supabase/database.types.ts`

**Interfaces:**
- Produces: `Raffle`, `RaffleTicket`, `RafflePurchase` entities; `RaffleRow`, `RaffleTicketRow`, `RafflePurchaseRow` row types

- [ ] **Step 1: Entidade Raffle**

```typescript
// src/core/domain/entities/raffle.ts
export type RaffleStatus = 'active' | 'sold_out' | 'drawing' | 'completed';

export interface Raffle {
  id: string;
  title: string;
  prizeName: string;
  prizeImageUrl: string | null;
  totalTickets: number;
  status: RaffleStatus;
  drawDate: string | null;
  winnerTicketId: string | null;
  createdAt: string;
}
```

- [ ] **Step 2: Entidade RaffleTicket**

```typescript
// src/core/domain/entities/raffle-ticket.ts
export type RaffleTicketStatus = 'available' | 'reserved' | 'paid';

export interface RaffleTicket {
  id: string;
  raffleId: string;
  ticketNumber: number;
  status: RaffleTicketStatus;
  reservedUntil: string | null;
  purchaseId: string | null;
  createdAt: string;
}
```

- [ ] **Step 3: Entidade RafflePurchase**

```typescript
// src/core/domain/entities/raffle-purchase.ts
export type RafflePurchaseStatus = 'pending' | 'paid' | 'failed' | 'expired';

export interface RafflePurchase {
  id: string;
  raffleId: string;
  stripeSessionId: string | null;
  ticketQuantity: number;
  amountCents: number;
  status: RafflePurchaseStatus;
  buyerName: string;
  buyerEmail: string;
  buyerPhone: string | null;
  confirmationToken: string;
  createdAt: string;
}
```

- [ ] **Step 4: Row types em database.types.ts**

Adicionar ao final de `src/infrastructure/supabase/database.types.ts`:

```typescript
export interface RaffleRow {
  id: string;
  title: string;
  prize_name: string;
  prize_image_url: string | null;
  total_tickets: number;
  status: 'active' | 'sold_out' | 'drawing' | 'completed';
  draw_date: string | null;
  winner_ticket_id: string | null;
  created_at: string;
}

export interface RaffleTicketRow {
  id: string;
  raffle_id: string;
  ticket_number: number;
  status: 'available' | 'reserved' | 'paid';
  reserved_until: string | null;
  purchase_id: string | null;
  created_at: string;
}

export interface RafflePurchaseRow {
  id: string;
  raffle_id: string;
  stripe_session_id: string | null;
  ticket_quantity: number;
  amount_cents: number;
  status: 'pending' | 'paid' | 'failed' | 'expired';
  buyer_name: string;
  buyer_email: string;
  buyer_phone: string | null;
  confirmation_token: string;
  created_at: string;
}
```

- [ ] **Step 5: Commit**

```bash
git add src/core/domain/entities/raffle.ts src/core/domain/entities/raffle-ticket.ts src/core/domain/entities/raffle-purchase.ts src/infrastructure/supabase/database.types.ts
git commit -m "feat(rifa): add raffle domain entities and row types"
```

---

## Task 3: Ports (Repository Interfaces)

**Files:**
- Create: `src/core/application/ports/raffle-repository.ts`
- Create: `src/core/application/ports/raffle-ticket-repository.ts`
- Create: `src/core/application/ports/raffle-purchase-repository.ts`

**Interfaces:**
- Consumes: `Raffle`, `RaffleTicket`, `RafflePurchase` entities (Task 2)
- Produces: `RaffleRepository`, `RaffleTicketRepository`, `RafflePurchaseRepository` interfaces

- [ ] **Step 1: RaffleRepository**

```typescript
// src/core/application/ports/raffle-repository.ts
import type { Raffle, RaffleStatus } from '@/core/domain/entities/raffle';

export interface RaffleRepository {
  findActive(): Promise<Raffle | null>;
  findById(id: string): Promise<Raffle | null>;
  updateStatus(id: string, status: RaffleStatus, drawDate?: string): Promise<void>;
  setWinner(id: string, winnerTicketId: string): Promise<void>;
  listDueForDraw(): Promise<Raffle[]>;
}
```

- [ ] **Step 2: RaffleTicketRepository**

```typescript
// src/core/application/ports/raffle-ticket-repository.ts
import type { RaffleTicket } from '@/core/domain/entities/raffle-ticket';

export interface ReservedTickets {
  tickets: RaffleTicket[];
}

export interface RaffleTicketRepository {
  /** Chama a RPC reserve_raffle_tickets via service-role. Lança erro se insuficiente. */
  reserveTickets(raffleId: string, quantity: number, purchaseId: string): Promise<ReservedTickets>;
  markAsPaid(purchaseId: string): Promise<void>;
  countByStatus(raffleId: string, status: 'available' | 'reserved' | 'paid'): Promise<number>;
  listByPurchase(purchaseId: string): Promise<RaffleTicket[]>;
  releaseExpired(): Promise<number>;
  findRandomPaid(raffleId: string): Promise<RaffleTicket | null>;
  countAvailable(raffleId: string): Promise<number>;
}
```

- [ ] **Step 3: RafflePurchaseRepository**

```typescript
// src/core/application/ports/raffle-purchase-repository.ts
import type { RafflePurchase, RafflePurchaseStatus } from '@/core/domain/entities/raffle-purchase';

export interface CreateRafflePurchaseInput {
  raffleId: string;
  ticketQuantity: number;
  amountCents: number;
  buyerName: string;
  buyerEmail: string;
  buyerPhone: string | null;
}

export interface RafflePurchaseRepository {
  create(input: CreateRafflePurchaseInput): Promise<RafflePurchase>;
  findBySessionId(sessionId: string): Promise<RafflePurchase | null>;
  findByToken(token: string): Promise<RafflePurchase | null>;
  updateStatus(id: string, status: RafflePurchaseStatus, sessionId?: string): Promise<void>;
  listByRaffle(raffleId: string): Promise<RafflePurchase[]>;
}
```

- [ ] **Step 4: Commit**

```bash
git add src/core/application/ports/raffle-repository.ts src/core/application/ports/raffle-ticket-repository.ts src/core/application/ports/raffle-purchase-repository.ts
git commit -m "feat(rifa): add raffle repository port interfaces"
```

---

## Task 4: Calculadora de Preço (função pura)

**Files:**
- Create: `src/core/application/use-cases/calculate-raffle-price.ts`

**Interfaces:**
- Produces: `calculateRafflePrice(quantity: number): number` (retorna centavos)

- [ ] **Step 1: Implementar**

```typescript
// src/core/application/use-cases/calculate-raffle-price.ts
import { DomainError } from '@/core/domain/errors/domain-error';

// Faixas: 1-2 → R$25, 3-4 → R$20, 5-9 → R$18, 10+ → R$15
const PRICE_TIERS: Array<{ maxQty: number; unitCents: number }> = [
  { maxQty: 2,        unitCents: 2500 },
  { maxQty: 4,        unitCents: 2000 },
  { maxQty: 9,        unitCents: 1800 },
  { maxQty: Infinity, unitCents: 1500 },
];

export function calculateRafflePrice(quantity: number): number {
  if (!Number.isInteger(quantity) || quantity < 1) {
    throw new DomainError('VALIDATION', 'Quantidade deve ser um número inteiro maior que zero');
  }
  const tier = PRICE_TIERS.find((t) => quantity <= t.maxQty)!;
  return tier.unitCents * quantity;
}
```

- [ ] **Step 2: Verificar manualmente no terminal**

```bash
# Abrir node REPL ou criar script temporário
node -e "
const { calculateRafflePrice } = require('./src/core/application/use-cases/calculate-raffle-price');
// Esperados: 2500, 6000, 9000, 15000
console.log(calculateRafflePrice(1));   // 2500
console.log(calculateRafflePrice(3));   // 6000
console.log(calculateRafflePrice(5));   // 9000
console.log(calculateRafflePrice(10));  // 15000
console.log(calculateRafflePrice(12));  // 18000
"
```

Nota: se `require` não funcionar com TypeScript, compile antes com `npx tsc --noEmit` para verificar tipos.

- [ ] **Step 3: Commit**

```bash
git add src/core/application/use-cases/calculate-raffle-price.ts
git commit -m "feat(rifa): add tier-based price calculator"
```

---

## Task 5: Repositories (Infrastructure)

**Files:**
- Create: `src/infrastructure/repositories/supabase-raffle-repository.ts`
- Create: `src/infrastructure/repositories/supabase-raffle-ticket-repository.ts`
- Create: `src/infrastructure/repositories/supabase-raffle-purchase-repository.ts`
- Modify: `src/infrastructure/repositories/mappers.ts`

**Interfaces:**
- Consumes: Ports (Task 3), Row types (Task 2)
- Produces: Implementações concretas dos 3 repositories

- [ ] **Step 1: Adicionar mappers**

Adicionar ao final de `src/infrastructure/repositories/mappers.ts`:

```typescript
import type { Raffle } from '@/core/domain/entities/raffle';
import type { RaffleTicket } from '@/core/domain/entities/raffle-ticket';
import type { RafflePurchase } from '@/core/domain/entities/raffle-purchase';
import type { RaffleRow, RaffleTicketRow, RafflePurchaseRow } from '@/infrastructure/supabase/database.types';

export function toRaffle(row: RaffleRow): Raffle {
  return {
    id: row.id,
    title: row.title,
    prizeName: row.prize_name,
    prizeImageUrl: row.prize_image_url,
    totalTickets: row.total_tickets,
    status: row.status,
    drawDate: row.draw_date,
    winnerTicketId: row.winner_ticket_id,
    createdAt: row.created_at,
  };
}

export function toRaffleTicket(row: RaffleTicketRow): RaffleTicket {
  return {
    id: row.id,
    raffleId: row.raffle_id,
    ticketNumber: row.ticket_number,
    status: row.status,
    reservedUntil: row.reserved_until,
    purchaseId: row.purchase_id,
    createdAt: row.created_at,
  };
}

export function toRafflePurchase(row: RafflePurchaseRow): RafflePurchase {
  return {
    id: row.id,
    raffleId: row.raffle_id,
    stripeSessionId: row.stripe_session_id,
    ticketQuantity: row.ticket_quantity,
    amountCents: row.amount_cents,
    status: row.status,
    buyerName: row.buyer_name,
    buyerEmail: row.buyer_email,
    buyerPhone: row.buyer_phone,
    confirmationToken: row.confirmation_token,
    createdAt: row.created_at,
  };
}
```

- [ ] **Step 2: SupabaseRaffleRepository**

```typescript
// src/infrastructure/repositories/supabase-raffle-repository.ts
import type { SupabaseClient } from '@supabase/supabase-js';
import type { Raffle, RaffleStatus } from '@/core/domain/entities/raffle';
import type { RaffleRepository } from '@/core/application/ports/raffle-repository';
import type { RaffleRow } from '@/infrastructure/supabase/database.types';
import { toRaffle } from './mappers';

export class SupabaseRaffleRepository implements RaffleRepository {
  constructor(private readonly db: SupabaseClient) {}

  async findActive(): Promise<Raffle | null> {
    const { data, error } = await this.db
      .from('raffles')
      .select('*')
      .in('status', ['active', 'sold_out', 'drawing'])
      .order('created_at', { ascending: false })
      .limit(1)
      .maybeSingle<RaffleRow>();
    if (error) throw error;
    return data ? toRaffle(data) : null;
  }

  async findById(id: string): Promise<Raffle | null> {
    const { data, error } = await this.db
      .from('raffles')
      .select('*')
      .eq('id', id)
      .maybeSingle<RaffleRow>();
    if (error) throw error;
    return data ? toRaffle(data) : null;
  }

  async updateStatus(id: string, status: RaffleStatus, drawDate?: string): Promise<void> {
    const { error } = await this.db
      .from('raffles')
      .update({ status, ...(drawDate ? { draw_date: drawDate } : {}) })
      .eq('id', id);
    if (error) throw error;
  }

  async setWinner(id: string, winnerTicketId: string): Promise<void> {
    const { error } = await this.db
      .from('raffles')
      .update({ winner_ticket_id: winnerTicketId, status: 'completed' })
      .eq('id', id);
    if (error) throw error;
  }

  async listDueForDraw(): Promise<Raffle[]> {
    const { data, error } = await this.db
      .from('raffles')
      .select('*')
      .eq('status', 'sold_out')
      .lte('draw_date', new Date().toISOString())
      .returns<RaffleRow[]>();
    if (error) throw error;
    return (data ?? []).map(toRaffle);
  }
}
```

- [ ] **Step 3: SupabaseRaffleTicketRepository**

```typescript
// src/infrastructure/repositories/supabase-raffle-ticket-repository.ts
import type { SupabaseClient } from '@supabase/supabase-js';
import type { RaffleTicket } from '@/core/domain/entities/raffle-ticket';
import type { RaffleTicketRepository, ReservedTickets } from '@/core/application/ports/raffle-ticket-repository';
import type { RaffleTicketRow } from '@/infrastructure/supabase/database.types';
import { toRaffleTicket } from './mappers';
import { DomainError } from '@/core/domain/errors/domain-error';

export class SupabaseRaffleTicketRepository implements RaffleTicketRepository {
  constructor(private readonly db: SupabaseClient) {}

  async reserveTickets(raffleId: string, quantity: number, purchaseId: string): Promise<ReservedTickets> {
    const { data, error } = await this.db.rpc('reserve_raffle_tickets', {
      p_raffle_id: raffleId,
      p_quantity: quantity,
      p_purchase_id: purchaseId,
    });
    if (error) {
      if (error.message.includes('INSUFFICIENT_TICKETS')) {
        throw new DomainError('CONFLICT', 'Bilhetes insuficientes disponíveis');
      }
      throw error;
    }
    const rows = (data ?? []) as Array<{ id: string; ticket_number: number }>;
    const tickets: RaffleTicket[] = rows.map((r) => ({
      id: r.id,
      raffleId,
      ticketNumber: r.ticket_number,
      status: 'reserved',
      reservedUntil: null,
      purchaseId,
      createdAt: new Date().toISOString(),
    }));
    return { tickets };
  }

  async markAsPaid(purchaseId: string): Promise<void> {
    const { error } = await this.db
      .from('raffle_tickets')
      .update({ status: 'paid', reserved_until: null })
      .eq('purchase_id', purchaseId);
    if (error) throw error;
  }

  async countByStatus(raffleId: string, status: 'available' | 'reserved' | 'paid'): Promise<number> {
    const { count, error } = await this.db
      .from('raffle_tickets')
      .select('id', { count: 'exact', head: true })
      .eq('raffle_id', raffleId)
      .eq('status', status);
    if (error) throw error;
    return count ?? 0;
  }

  async listByPurchase(purchaseId: string): Promise<RaffleTicket[]> {
    const { data, error } = await this.db
      .from('raffle_tickets')
      .select('*')
      .eq('purchase_id', purchaseId)
      .order('ticket_number', { ascending: true })
      .returns<RaffleTicketRow[]>();
    if (error) throw error;
    return (data ?? []).map(toRaffleTicket);
  }

  async releaseExpired(): Promise<number> {
    const { data, error } = await this.db
      .from('raffle_tickets')
      .update({ status: 'available', reserved_until: null, purchase_id: null })
      .eq('status', 'reserved')
      .lt('reserved_until', new Date().toISOString())
      .select('id');
    if (error) throw error;
    return (data ?? []).length;
  }

  async findRandomPaid(raffleId: string): Promise<RaffleTicket | null> {
    // Busca TODOS os pagos e sorteia no JS — seguro para 200 bilhetes
    const { data, error } = await this.db
      .from('raffle_tickets')
      .select('*')
      .eq('raffle_id', raffleId)
      .eq('status', 'paid')
      .returns<RaffleTicketRow[]>();
    if (error) throw error;
    if (!data || data.length === 0) return null;
    const idx = Math.floor(Math.random() * data.length);
    return toRaffleTicket(data[idx]);
  }

  async countAvailable(raffleId: string): Promise<number> {
    const { count, error } = await this.db
      .from('raffle_tickets')
      .select('id', { count: 'exact', head: true })
      .eq('raffle_id', raffleId)
      .or(`status.eq.available,and(status.eq.reserved,reserved_until.lt.${new Date().toISOString()})`);
    if (error) throw error;
    return count ?? 0;
  }
}
```

> **Nota:** `findRandomPaid` busca todos os bilhetes `paid` e sorteia no JS. Para 200 bilhetes isso é seguro. Se a rifa escalar para milhares, substituir por RPC PostgreSQL com `ORDER BY random() LIMIT 1`.

- [ ] **Step 4: SupabaseRafflePurchaseRepository**

```typescript
// src/infrastructure/repositories/supabase-raffle-purchase-repository.ts
import type { SupabaseClient } from '@supabase/supabase-js';
import type { RafflePurchase, RafflePurchaseStatus } from '@/core/domain/entities/raffle-purchase';
import type { RafflePurchaseRepository, CreateRafflePurchaseInput } from '@/core/application/ports/raffle-purchase-repository';
import type { RafflePurchaseRow } from '@/infrastructure/supabase/database.types';
import { toRafflePurchase } from './mappers';

export class SupabaseRafflePurchaseRepository implements RafflePurchaseRepository {
  constructor(private readonly db: SupabaseClient) {}

  async create(input: CreateRafflePurchaseInput): Promise<RafflePurchase> {
    const { data, error } = await this.db
      .from('raffle_purchases')
      .insert({
        raffle_id: input.raffleId,
        ticket_quantity: input.ticketQuantity,
        amount_cents: input.amountCents,
        buyer_name: input.buyerName,
        buyer_email: input.buyerEmail,
        buyer_phone: input.buyerPhone,
      })
      .select('*')
      .single<RafflePurchaseRow>();
    if (error) throw error;
    return toRafflePurchase(data);
  }

  async findBySessionId(sessionId: string): Promise<RafflePurchase | null> {
    const { data, error } = await this.db
      .from('raffle_purchases')
      .select('*')
      .eq('stripe_session_id', sessionId)
      .maybeSingle<RafflePurchaseRow>();
    if (error) throw error;
    return data ? toRafflePurchase(data) : null;
  }

  async findByToken(token: string): Promise<RafflePurchase | null> {
    const { data, error } = await this.db
      .from('raffle_purchases')
      .select('*')
      .eq('confirmation_token', token)
      .maybeSingle<RafflePurchaseRow>();
    if (error) throw error;
    return data ? toRafflePurchase(data) : null;
  }

  async updateStatus(id: string, status: RafflePurchaseStatus, sessionId?: string): Promise<void> {
    const { error } = await this.db
      .from('raffle_purchases')
      .update({ status, ...(sessionId ? { stripe_session_id: sessionId } : {}) })
      .eq('id', id);
    if (error) throw error;
  }

  async listByRaffle(raffleId: string): Promise<RafflePurchase[]> {
    const { data, error } = await this.db
      .from('raffle_purchases')
      .select('*')
      .eq('raffle_id', raffleId)
      .order('created_at', { ascending: false })
      .returns<RafflePurchaseRow[]>();
    if (error) throw error;
    return (data ?? []).map(toRafflePurchase);
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add src/infrastructure/repositories/mappers.ts src/infrastructure/repositories/supabase-raffle-repository.ts src/infrastructure/repositories/supabase-raffle-ticket-repository.ts src/infrastructure/repositories/supabase-raffle-purchase-repository.ts
git commit -m "feat(rifa): add raffle Supabase repositories and mappers"
```

---

## Task 6: Use Cases

**Files:**
- Create: `src/core/application/use-cases/purchase-raffle-tickets.ts`
- Create: `src/core/application/use-cases/confirm-raffle-payment.ts`
- Create: `src/core/application/use-cases/release-expired-reservations.ts`
- Create: `src/core/application/use-cases/draw-raffle-winner.ts`

**Interfaces:**
- Consumes: Ports (Task 3), `calculateRafflePrice` (Task 4)
- Produces: 4 use case classes

- [ ] **Step 1: PurchaseRaffleTickets**

```typescript
// src/core/application/use-cases/purchase-raffle-tickets.ts
import type Stripe from 'stripe';
import { DomainError } from '@/core/domain/errors/domain-error';
import { calculateRafflePrice } from './calculate-raffle-price';
import type { RaffleRepository } from '@/core/application/ports/raffle-repository';
import type { RaffleTicketRepository } from '@/core/application/ports/raffle-ticket-repository';
import type { RafflePurchaseRepository } from '@/core/application/ports/raffle-purchase-repository';
import type { Logger } from '@/core/application/ports/logger';

export interface PurchaseRaffleTicketsInput {
  raffleId: string;
  quantity: number;
  buyerName: string;
  buyerEmail: string;
  buyerPhone: string | null;
  siteUrl: string;
}

export interface PurchaseRaffleTicketsResult {
  sessionUrl: string;
  confirmationToken: string;
}

export class PurchaseRaffleTickets {
  constructor(
    private readonly raffles: RaffleRepository,
    private readonly tickets: RaffleTicketRepository,
    private readonly purchases: RafflePurchaseRepository,
    private readonly stripe: Stripe,
    private readonly logger: Logger,
  ) {}

  async execute(input: PurchaseRaffleTicketsInput): Promise<PurchaseRaffleTicketsResult> {
    const log = this.logger.child({ scope: 'purchase-raffle-tickets', raffleId: input.raffleId });

    const raffle = await this.raffles.findById(input.raffleId);
    if (!raffle) throw new DomainError('NOT_FOUND', 'Rifa não encontrada');
    if (raffle.status !== 'active') throw new DomainError('CONFLICT', 'Rifa não está mais aceitando compras');

    if (!Number.isInteger(input.quantity) || input.quantity < 1) {
      throw new DomainError('VALIDATION', 'Quantidade inválida');
    }

    const available = await this.tickets.countAvailable(input.raffleId);
    if (available < input.quantity) {
      throw new DomainError('CONFLICT', `Apenas ${available} bilhete${available !== 1 ? 's' : ''} disponível${available !== 1 ? 'is' : ''}`);
    }

    const amountCents = calculateRafflePrice(input.quantity);

    // Cria purchase (pending) antes de reservar bilhetes
    const purchase = await this.purchases.create({
      raffleId: input.raffleId,
      ticketQuantity: input.quantity,
      amountCents,
      buyerName: input.buyerName,
      buyerEmail: input.buyerEmail,
      buyerPhone: input.buyerPhone,
    });

    // Reserva atômica via RPC
    const { tickets } = await this.tickets.reserveTickets(input.raffleId, input.quantity, purchase.id);
    const ticketNumbers = tickets.map((t) => t.ticketNumber).sort((a, b) => a - b);

    log.info('raffle.tickets_reserved', { purchaseId: purchase.id, tickets: ticketNumbers });

    // Cria Stripe Checkout Session com price_data (valor dinâmico)
    const session = await this.stripe.checkout.sessions.create({
      mode: 'payment',
      line_items: [{
        price_data: {
          currency: 'brl',
          unit_amount: amountCents,
          product_data: {
            name: `Rifa — ${input.quantity} bilhete${input.quantity > 1 ? 's' : ''}`,
            description: `Bilhete${input.quantity > 1 ? 's' : ''}: ${ticketNumbers.join(', ')}`,
          },
        },
        quantity: 1,
      }],
      customer_email: input.buyerEmail,
      phone_number_collection: { enabled: true },
      success_url: `${input.siteUrl}/rifa/confirmacao/${purchase.confirmationToken}`,
      cancel_url: `${input.siteUrl}/rifa`,
      metadata: {
        type: 'raffle',
        raffle_id: input.raffleId,
        purchase_id: purchase.id,
      },
      payment_intent_data: {
        metadata: { type: 'raffle', purchase_id: purchase.id },
      },
    });

    if (!session.url) throw new DomainError('PAYMENT', 'Stripe não retornou URL de checkout');

    // Associa session_id à purchase
    await this.purchases.updateStatus(purchase.id, 'pending', session.id);

    return { sessionUrl: session.url, confirmationToken: purchase.confirmationToken };
  }
}
```

- [ ] **Step 2: ConfirmRafflePayment**

```typescript
// src/core/application/use-cases/confirm-raffle-payment.ts
import { DomainError } from '@/core/domain/errors/domain-error';
import type { RaffleRepository } from '@/core/application/ports/raffle-repository';
import type { RaffleTicketRepository } from '@/core/application/ports/raffle-ticket-repository';
import type { RafflePurchaseRepository } from '@/core/application/ports/raffle-purchase-repository';
import type { Logger } from '@/core/application/ports/logger';

export interface ConfirmRafflePaymentInput {
  stripeSessionId: string;
}

export class ConfirmRafflePayment {
  constructor(
    private readonly raffles: RaffleRepository,
    private readonly tickets: RaffleTicketRepository,
    private readonly purchases: RafflePurchaseRepository,
    private readonly logger: Logger,
  ) {}

  async execute(input: ConfirmRafflePaymentInput): Promise<void> {
    const log = this.logger.child({ scope: 'confirm-raffle-payment' });

    const purchase = await this.purchases.findBySessionId(input.stripeSessionId);
    if (!purchase) {
      log.warn('raffle.purchase_not_found', { sessionId: input.stripeSessionId });
      return;
    }
    if (purchase.status === 'paid') {
      log.info('raffle.purchase_already_paid', { purchaseId: purchase.id });
      return;
    }

    // Marca bilhetes como pagos e purchase como paga
    await this.tickets.markAsPaid(purchase.id);
    await this.purchases.updateStatus(purchase.id, 'paid');

    log.info('raffle.payment_confirmed', { purchaseId: purchase.id, quantity: purchase.ticketQuantity });

    // Checa se todos os bilhetes foram vendidos
    const available = await this.tickets.countAvailable(purchase.raffleId);
    if (available === 0) {
      const drawDate = new Date(Date.now() + 5 * 24 * 60 * 60 * 1000).toISOString();
      await this.raffles.updateStatus(purchase.raffleId, 'sold_out', drawDate);
      log.info('raffle.sold_out', { raffleId: purchase.raffleId, drawDate });
    }
  }
}
```

- [ ] **Step 3: ReleaseExpiredReservations**

```typescript
// src/core/application/use-cases/release-expired-reservations.ts
import type { RaffleTicketRepository } from '@/core/application/ports/raffle-ticket-repository';
import type { Logger } from '@/core/application/ports/logger';

export class ReleaseExpiredReservations {
  constructor(
    private readonly tickets: RaffleTicketRepository,
    private readonly logger: Logger,
  ) {}

  async execute(): Promise<void> {
    const released = await this.tickets.releaseExpired();
    if (released > 0) {
      this.logger.info('raffle.reservations_released', { count: released });
    }
  }
}
```

- [ ] **Step 4: DrawRaffleWinner**

```typescript
// src/core/application/use-cases/draw-raffle-winner.ts
import { DomainError } from '@/core/domain/errors/domain-error';
import type { RaffleRepository } from '@/core/application/ports/raffle-repository';
import type { RaffleTicketRepository } from '@/core/application/ports/raffle-ticket-repository';
import type { RafflePurchaseRepository } from '@/core/application/ports/raffle-purchase-repository';
import type { Logger } from '@/core/application/ports/logger';

export interface DrawResult {
  winnerTicketNumber: number;
  winnerName: string;
  winnerEmail: string;
}

export class DrawRaffleWinner {
  constructor(
    private readonly raffles: RaffleRepository,
    private readonly tickets: RaffleTicketRepository,
    private readonly purchases: RafflePurchaseRepository,
    private readonly logger: Logger,
  ) {}

  async execute(raffleId: string): Promise<DrawResult> {
    const log = this.logger.child({ scope: 'draw-raffle-winner', raffleId });

    const raffle = await this.raffles.findById(raffleId);
    if (!raffle) throw new DomainError('NOT_FOUND', 'Rifa não encontrada');
    if (raffle.status === 'completed') throw new DomainError('CONFLICT', 'Rifa já foi sorteada');
    if (raffle.status !== 'sold_out') throw new DomainError('CONFLICT', 'Rifa ainda não está encerrada');

    const winner = await this.tickets.findRandomPaid(raffleId);
    if (!winner) throw new DomainError('INTERNAL', 'Nenhum bilhete pago encontrado para sorteio');

    await this.raffles.setWinner(raffleId, winner.id);

    log.info('raffle.winner_drawn', {
      raffleId,
      ticketNumber: winner.ticketNumber,
      winnerEmail: winner.buyerEmail,
    });

    // Busca dados do comprador via purchase
    const winnerPurchase = winner.purchaseId
      ? (await this.purchases.listByRaffle(raffleId)).find((p) => p.id === winner.purchaseId)
      : null;

    return {
      winnerTicketNumber: winner.ticketNumber,
      winnerName: winnerPurchase?.buyerName ?? 'Ganhador',
      winnerEmail: winnerPurchase?.buyerEmail ?? '',
    };
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add src/core/application/use-cases/purchase-raffle-tickets.ts src/core/application/use-cases/confirm-raffle-payment.ts src/core/application/use-cases/release-expired-reservations.ts src/core/application/use-cases/draw-raffle-winner.ts
git commit -m "feat(rifa): add raffle use cases (purchase, confirm, release, draw)"
```

---

## Task 7: Container (DI) — Wiring

**Files:**
- Modify: `src/infrastructure/di/container.ts`

**Interfaces:**
- Consumes: todos os repositories (Task 5) e use cases (Task 6)
- Produces: `raffleTickets`, `rafflePurchases`, `raffleRepo` nos repos; `purchaseRaffleTickets`, `confirmRafflePayment`, `releaseExpiredReservations`, `drawRaffleWinner` nos containers

- [ ] **Step 1: Atualizar container.ts**

Adicionar os imports no topo (após os imports existentes):

```typescript
import { SupabaseRaffleRepository } from '@/infrastructure/repositories/supabase-raffle-repository';
import { SupabaseRaffleTicketRepository } from '@/infrastructure/repositories/supabase-raffle-ticket-repository';
import { SupabaseRafflePurchaseRepository } from '@/infrastructure/repositories/supabase-raffle-purchase-repository';
import { PurchaseRaffleTickets } from '@/core/application/use-cases/purchase-raffle-tickets';
import { ConfirmRafflePayment } from '@/core/application/use-cases/confirm-raffle-payment';
import { ReleaseExpiredReservations } from '@/core/application/use-cases/release-expired-reservations';
import { DrawRaffleWinner } from '@/core/application/use-cases/draw-raffle-winner';
```

Adicionar ao `buildRepos()`:

```typescript
raffleRepo: new SupabaseRaffleRepository(db),
raffleTickets: new SupabaseRaffleTicketRepository(db),
rafflePurchases: new SupabaseRafflePurchaseRepository(db),
```

Adicionar ao `adminContainer()` (a RPC `reserve_raffle_tickets` é `service_role` only):

```typescript
get purchaseRaffleTickets() {
  return new PurchaseRaffleTickets(
    repos.raffleRepo,
    repos.raffleTickets,
    repos.rafflePurchases,
    getStripe(),
    logger,
  );
},
confirmRafflePayment: new ConfirmRafflePayment(
  repos.raffleRepo,
  repos.raffleTickets,
  repos.rafflePurchases,
  logger,
),
releaseExpiredReservations: new ReleaseExpiredReservations(repos.raffleTickets, logger),
drawRaffleWinner: new DrawRaffleWinner(
  repos.raffleRepo,
  repos.raffleTickets,
  repos.rafflePurchases,
  logger,
),
```

- [ ] **Step 2: Verificar tipos**

```bash
npx tsc --noEmit
```

Corrigir quaisquer erros de tipo antes de continuar.

- [ ] **Step 3: Commit**

```bash
git add src/infrastructure/di/container.ts
git commit -m "feat(rifa): wire raffle repos and use cases into DI container"
```

---

## Task 8: API Routes

**Files:**
- Create: `src/app/api/raffle/checkout/route.ts`
- Create: `src/app/api/raffle/[id]/status/route.ts`
- Create: `src/app/api/raffle/confirmacao/[token]/route.ts`
- Modify: `src/app/api/stripe/webhook/route.ts`

**Interfaces:**
- Consumes: `userScopedContainer().purchaseRaffleTickets`, `adminContainer().confirmRafflePayment`

- [ ] **Step 1: POST /api/raffle/checkout**

```typescript
// src/app/api/raffle/checkout/route.ts
import { NextResponse } from 'next/server';
import { z } from 'zod';
import { adminContainer, logger } from '@/infrastructure/di/container';
import { DomainError, httpStatusFor } from '@/core/domain/errors/domain-error';
import { publicEnv } from '@/lib/env';

export const runtime = 'nodejs';

const schema = z.object({
  raffle_id: z.string().uuid(),
  quantity: z.number().int().min(1).max(200),
  buyer_name: z.string().min(1).max(200),
  buyer_email: z.string().email(),
  buyer_phone: z.string().nullable().optional(),
});

export async function POST(request: Request) {
  const log = logger.child({ scope: 'raffle.checkout' });
  try {
    const body = await request.json();
    const parsed = schema.safeParse(body);
    if (!parsed.success) {
      return NextResponse.json({ error: 'Dados inválidos', details: parsed.error.flatten() }, { status: 422 });
    }

    const { raffle_id, quantity, buyer_name, buyer_email, buyer_phone } = parsed.data;
    // adminContainer: a RPC de reserva requer service_role
    const c = adminContainer();
    const result = await c.purchaseRaffleTickets.execute({
      raffleId: raffle_id,
      quantity,
      buyerName: buyer_name,
      buyerEmail: buyer_email,
      buyerPhone: buyer_phone ?? null,
      siteUrl: publicEnv.NEXT_PUBLIC_SITE_URL,
    });

    return NextResponse.json({ session_url: result.sessionUrl });
  } catch (err) {
    if (err instanceof DomainError) {
      log.warn('raffle.checkout_error', { code: err.code, message: err.message });
      return NextResponse.json({ error: err.message }, { status: httpStatusFor[err.code] });
    }
    log.error('raffle.checkout_unexpected', { error: String(err) });
    return NextResponse.json({ error: 'Erro interno' }, { status: 500 });
  }
}
```

- [ ] **Step 2: GET /api/raffle/[id]/status**

```typescript
// src/app/api/raffle/[id]/status/route.ts
import { NextResponse } from 'next/server';
import { userScopedContainer } from '@/infrastructure/di/container';

export const runtime = 'nodejs';

export async function GET(_req: Request, { params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const c = await userScopedContainer();
  const raffle = await c.raffleRepo.findById(id);
  if (!raffle) return NextResponse.json({ error: 'Não encontrada' }, { status: 404 });

  const paid = await c.raffleTickets.countByStatus(id, 'paid');
  const reserved = await c.raffleTickets.countByStatus(id, 'reserved');

  return NextResponse.json({
    status: raffle.status,
    total_tickets: raffle.totalTickets,
    paid_tickets: paid,
    reserved_tickets: reserved,
    available_tickets: raffle.totalTickets - paid - reserved,
    draw_date: raffle.drawDate,
    winner_ticket_id: raffle.winnerTicketId,
  });
}
```

- [ ] **Step 3: GET /api/raffle/confirmacao/[token]**

```typescript
// src/app/api/raffle/confirmacao/[token]/route.ts
import { NextResponse } from 'next/server';
import { userScopedContainer } from '@/infrastructure/di/container';

export const runtime = 'nodejs';

export async function GET(_req: Request, { params }: { params: Promise<{ token: string }> }) {
  const { token } = await params;
  const c = await userScopedContainer();
  const purchase = await c.rafflePurchases.findByToken(token);
  if (!purchase || purchase.status !== 'paid') {
    return NextResponse.json({ error: 'Compra não encontrada' }, { status: 404 });
  }
  const tickets = await c.raffleTickets.listByPurchase(purchase.id);
  return NextResponse.json({
    buyer_name: purchase.buyerName,
    ticket_quantity: purchase.ticketQuantity,
    amount_cents: purchase.amountCents,
    ticket_numbers: tickets.map((t) => t.ticketNumber).sort((a, b) => a - b),
    created_at: purchase.createdAt,
  });
}
```

- [ ] **Step 4: Estender webhook Stripe**

No arquivo `src/app/api/stripe/webhook/route.ts`, dentro do `switch (event.type)`, após o bloco `case 'checkout.session.completed':` existente, adicionar o tratamento de rifa:

Modificar o case existente `checkout.session.completed` para incluir o ramo de rifa:

```typescript
case 'checkout.session.completed': {
  const session = event.data.object;
  if (session.mode === 'payment' && session.payment_status === 'paid') {
    // ── RIFA ──────────────────────────────────────────────────────────
    if (session.metadata?.type === 'raffle') {
      await c.confirmRafflePayment.execute({ stripeSessionId: session.id });
      break;
    }
    // ── PRODUTO NORMAL ────────────────────────────────────────────────
    const productId = session.metadata?.product_id;
    const email = session.customer_details?.email ?? session.customer_email;
    if (productId && email) {
      await c.handleCheckoutCompleted.execute({
        sessionId: session.id,
        paymentIntent:
          typeof session.payment_intent === 'string'
            ? session.payment_intent
            : (session.payment_intent?.id ?? null),
        productId,
        customerEmail: email,
        customerName: session.customer_details?.name ?? null,
        customerPhone: session.customer_details?.phone ?? null,
        amountTotal: session.amount_total ?? 0,
        currency: session.currency ?? 'brl',
      });
    }
  }
  if (session.mode === 'subscription') {
    await c.handleSubscriptionWebhook.handleCheckoutCompleted(session);
  }
  break;
}
```

- [ ] **Step 5: Verificar tipos**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add src/app/api/raffle/ src/app/api/stripe/webhook/route.ts
git commit -m "feat(rifa): add raffle API routes and stripe webhook handler"
```

---

## Task 9: Cron Jobs

**Files:**
- Create: `src/app/api/cron/raffle-draw/route.ts`
- Create: `src/app/api/cron/raffle-cleanup/route.ts`
- Modify: `vercel.json`

- [ ] **Step 1: Cron raffle-draw**

```typescript
// src/app/api/cron/raffle-draw/route.ts
import { NextResponse } from 'next/server';
import { adminContainer, logger } from '@/infrastructure/di/container';

export const runtime = 'nodejs';

export async function GET(request: Request) {
  const auth = request.headers.get('authorization');
  if (auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const log = logger.child({ scope: 'cron.raffle-draw' });
  const c = adminContainer();

  const rafflesDue = await c.raffleRepo.listDueForDraw();
  log.info('cron.raffle_draw_start', { count: rafflesDue.length });

  const results: Array<{ raffleId: string; ticketNumber?: number; error?: string }> = [];

  for (const raffle of rafflesDue) {
    try {
      const result = await c.drawRaffleWinner.execute(raffle.id);
      log.info('cron.raffle_drawn', { raffleId: raffle.id, winner: result.winnerTicketNumber });
      results.push({ raffleId: raffle.id, ticketNumber: result.winnerTicketNumber });
    } catch (err) {
      const message = err instanceof Error ? err.message : String(err);
      log.error('cron.raffle_draw_failed', { raffleId: raffle.id, error: message });
      results.push({ raffleId: raffle.id, error: message });
    }
  }

  return NextResponse.json({ processed: results.length, results });
}
```

- [ ] **Step 2: Cron raffle-cleanup**

```typescript
// src/app/api/cron/raffle-cleanup/route.ts
import { NextResponse } from 'next/server';
import { adminContainer, logger } from '@/infrastructure/di/container';

export const runtime = 'nodejs';

export async function GET(request: Request) {
  const auth = request.headers.get('authorization');
  if (auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const log = logger.child({ scope: 'cron.raffle-cleanup' });
  const c = adminContainer();

  await c.releaseExpiredReservations.execute();
  log.info('cron.cleanup_done');
  return NextResponse.json({ ok: true });
}
```

- [ ] **Step 3: Atualizar vercel.json**

```json
{
  "crons": [
    {
      "path": "/api/keepalive",
      "schedule": "0 12 * * *"
    },
    {
      "path": "/api/cron/raffle-draw",
      "schedule": "0 13 * * *"
    },
    {
      "path": "/api/cron/raffle-cleanup",
      "schedule": "0 * * * *"
    }
  ]
}
```

> Horário: `0 13 * * *` = 13:00 UTC = 10:00 BRT (UTC-3). Cleanup a cada hora.

- [ ] **Step 4: Adicionar CRON_SECRET às env vars**

Adicionar ao `serverSchema` em `src/lib/env.ts`:
```typescript
CRON_SECRET: z.string().min(1),
```

E ao `serverEnv()`:
```typescript
CRON_SECRET: process.env.CRON_SECRET ?? '',
```

Definir `CRON_SECRET` no Vercel Dashboard (Settings → Environment Variables) com um valor aleatório forte. Localmente, adicionar ao `.env.local`:
```
CRON_SECRET=qualquer-string-longa-aleatoria
```

- [ ] **Step 5: Commit**

```bash
git add src/app/api/cron/ vercel.json src/lib/env.ts
git commit -m "feat(rifa): add raffle draw and cleanup cron jobs"
```

---

## Task 10: Página Pública /rifa

**Files:**
- Create: `src/app/rifa/page.tsx`

**Interfaces:**
- Consumes: `GET /api/raffle/[id]/status`, `POST /api/raffle/checkout`
- Produces: página pública com input de quantidade, preview de preço, botão de compra

- [ ] **Step 1: Implementar página**

```tsx
// src/app/rifa/page.tsx
import { userScopedContainer } from '@/infrastructure/di/container';
import RaffleClient from './raffle-client';
import { notFound } from 'next/navigation';

export const dynamic = 'force-dynamic';

export default async function RifaPage() {
  const c = await userScopedContainer();
  const raffle = await c.raffleRepo.findActive();
  if (!raffle) notFound();

  const paid = await c.raffleTickets.countByStatus(raffle.id, 'paid');
  const reserved = await c.raffleTickets.countByStatus(raffle.id, 'reserved');
  const available = raffle.totalTickets - paid - reserved;

  return (
    <RaffleClient
      raffle={{
        id: raffle.id,
        title: raffle.title,
        prizeName: raffle.prizeName,
        prizeImageUrl: raffle.prizeImageUrl,
        totalTickets: raffle.totalTickets,
        status: raffle.status,
        drawDate: raffle.drawDate,
      }}
      initialCounts={{ paid, reserved, available }}
    />
  );
}
```

Criar também `src/app/rifa/raffle-client.tsx` (componente Client):

```tsx
// src/app/rifa/raffle-client.tsx
'use client';

import { useState, useEffect } from 'react';

interface RaffleProps {
  raffle: {
    id: string;
    title: string;
    prizeName: string;
    prizeImageUrl: string | null;
    totalTickets: number;
    status: string;
    drawDate: string | null;
  };
  initialCounts: { paid: number; reserved: number; available: number };
}

const TIERS = [
  { maxQty: 2,        unitCents: 2500, label: '1-2 bilhetes' },
  { maxQty: 4,        unitCents: 2000, label: '3-4 bilhetes' },
  { maxQty: 9,        unitCents: 1800, label: '5-9 bilhetes' },
  { maxQty: Infinity, unitCents: 1500, label: '10+ bilhetes' },
];

function calcPrice(qty: number): number {
  const tier = TIERS.find((t) => qty <= t.maxQty)!;
  return tier.unitCents * qty;
}

function fmt(cents: number) {
  return (cents / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });
}

export default function RaffleClient({ raffle, initialCounts }: RaffleProps) {
  const [counts, setCounts] = useState(initialCounts);
  const [quantity, setQuantity] = useState(1);
  const [name, setName] = useState('');
  const [email, setEmail] = useState('');
  const [phone, setPhone] = useState('');
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const isSoldOut = raffle.status !== 'active' || counts.available <= 0;
  const maxQty = Math.min(counts.available, 200);
  const price = calcPrice(Math.max(1, quantity));
  const unitPrice = calcPrice(Math.max(1, quantity)) / Math.max(1, quantity);

  // Polling a cada 30s
  useEffect(() => {
    const interval = setInterval(async () => {
      const res = await fetch(`/api/raffle/${raffle.id}/status`);
      if (res.ok) {
        const data = await res.json();
        setCounts({
          paid: data.paid_tickets,
          reserved: data.reserved_tickets,
          available: data.available_tickets,
        });
      }
    }, 30_000);
    return () => clearInterval(interval);
  }, [raffle.id]);

  async function handleBuy() {
    setError(null);
    if (!name.trim() || !email.trim()) {
      setError('Preencha nome e e-mail');
      return;
    }
    setLoading(true);
    try {
      const res = await fetch('/api/raffle/checkout', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          raffle_id: raffle.id,
          quantity,
          buyer_name: name.trim(),
          buyer_email: email.trim(),
          buyer_phone: phone.trim() || null,
        }),
      });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? 'Erro ao criar checkout');
        return;
      }
      window.location.href = data.session_url;
    } catch {
      setError('Erro de conexão. Tente novamente.');
    } finally {
      setLoading(false);
    }
  }

  const soldPct = Math.round((counts.paid / raffle.totalTickets) * 100);

  return (
    <main className="min-h-screen bg-[#0a0a0a] text-white">
      <div className="mx-auto max-w-2xl px-4 py-12">
        {/* Header */}
        <div className="mb-8 text-center">
          <span className="inline-block rounded-full bg-[#FF4D2D]/20 px-3 py-1 text-xs font-bold uppercase tracking-widest text-[#FF4D2D]">
            Rifa Oficial
          </span>
          <h1 className="mt-3 text-3xl font-bold">{raffle.title}</h1>
          <p className="mt-1 text-[#8B92A8]">Prêmio: {raffle.prizeName}</p>
        </div>

        {/* Imagem do prêmio */}
        {raffle.prizeImageUrl && (
          <div className="mb-8 overflow-hidden rounded-2xl">
            <img src={raffle.prizeImageUrl} alt={raffle.prizeName} className="w-full object-cover" />
          </div>
        )}

        {/* Progresso */}
        <div className="mb-8 rounded-2xl bg-white/5 p-6">
          <div className="mb-3 flex items-center justify-between text-sm">
            <span className="font-medium">{counts.paid} bilhetes vendidos</span>
            <span className="text-[#8B92A8]">{counts.available} disponíveis</span>
          </div>
          <div className="h-3 overflow-hidden rounded-full bg-white/10">
            <div
              className="h-full rounded-full bg-[#FF4D2D] transition-all duration-500"
              style={{ width: `${soldPct}%` }}
            />
          </div>
          <p className="mt-2 text-center text-xs text-[#8B92A8]">
            {raffle.totalTickets} bilhetes no total
          </p>
          {raffle.drawDate && (
            <p className="mt-2 text-center text-sm font-medium text-[#FF4D2D]">
              Sorteio em {new Date(raffle.drawDate).toLocaleDateString('pt-BR', {
                day: '2-digit', month: 'long', year: 'numeric',
              })}
            </p>
          )}
        </div>

        {isSoldOut ? (
          <div className="rounded-2xl bg-white/5 p-6 text-center">
            <p className="text-lg font-bold">Todos os bilhetes foram vendidos!</p>
            {raffle.drawDate && (
              <p className="mt-2 text-[#8B92A8]">
                O sorteio acontece em{' '}
                {new Date(raffle.drawDate).toLocaleDateString('pt-BR', {
                  day: '2-digit', month: 'long', year: 'numeric',
                })}
              </p>
            )}
          </div>
        ) : (
          <div className="rounded-2xl bg-white/5 p-6">
            <h2 className="mb-6 text-lg font-bold">Comprar bilhetes</h2>

            {/* Faixas de preço */}
            <div className="mb-6 grid grid-cols-2 gap-2 text-sm sm:grid-cols-4">
              {[1, 3, 5, 10].map((q) => (
                <button
                  key={q}
                  onClick={() => setQuantity(q)}
                  className={`rounded-xl px-3 py-2 text-center transition ${
                    quantity === q
                      ? 'bg-[#FF4D2D] font-bold text-white'
                      : 'bg-white/10 text-[#8B92A8] hover:bg-white/20'
                  }`}
                >
                  {q} {q === 1 ? 'bilhete' : 'bilhetes'}
                  <div className="text-xs opacity-75">{fmt(calcPrice(q))}</div>
                </button>
              ))}
            </div>

            {/* Input de quantidade */}
            <div className="mb-6">
              <label className="mb-2 block text-sm text-[#8B92A8]">
                Ou digite a quantidade
              </label>
              <input
                type="number"
                min={1}
                max={maxQty}
                value={quantity}
                onChange={(e) => {
                  const v = Math.max(1, Math.min(maxQty, parseInt(e.target.value) || 1));
                  setQuantity(v);
                }}
                className="w-full rounded-xl bg-white/10 px-4 py-3 text-white outline-none focus:ring-2 focus:ring-[#FF4D2D]"
              />
              <div className="mt-2 flex items-center justify-between text-sm">
                <span className="text-[#8B92A8]">{fmt(unitPrice / 100)} por bilhete</span>
                <span className="text-lg font-bold text-[#FF4D2D]">Total: {fmt(price)}</span>
              </div>
            </div>

            {/* Dados do comprador */}
            <div className="mb-6 space-y-3">
              <input
                type="text"
                placeholder="Seu nome"
                value={name}
                onChange={(e) => setName(e.target.value)}
                className="w-full rounded-xl bg-white/10 px-4 py-3 text-white placeholder:text-[#8B92A8] outline-none focus:ring-2 focus:ring-[#FF4D2D]"
              />
              <input
                type="email"
                placeholder="Seu e-mail"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                className="w-full rounded-xl bg-white/10 px-4 py-3 text-white placeholder:text-[#8B92A8] outline-none focus:ring-2 focus:ring-[#FF4D2D]"
              />
              <input
                type="tel"
                placeholder="WhatsApp (opcional)"
                value={phone}
                onChange={(e) => setPhone(e.target.value)}
                className="w-full rounded-xl bg-white/10 px-4 py-3 text-white placeholder:text-[#8B92A8] outline-none focus:ring-2 focus:ring-[#FF4D2D]"
              />
            </div>

            {error && (
              <p className="mb-4 rounded-xl bg-red-500/20 px-4 py-3 text-sm text-red-400">{error}</p>
            )}

            <button
              onClick={handleBuy}
              disabled={loading || isSoldOut}
              className="w-full rounded-xl bg-[#FF4D2D] px-6 py-4 text-lg font-bold text-white transition hover:bg-[#e53e21] disabled:cursor-not-allowed disabled:opacity-50"
            >
              {loading ? 'Aguarde...' : `Comprar ${quantity} bilhete${quantity > 1 ? 's' : ''} — ${fmt(price)}`}
            </button>

            <p className="mt-4 text-center text-xs text-[#8B92A8]">
              Pagamento seguro via Stripe • Cartão ou PIX
            </p>
          </div>
        )}
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Corrigir bug no unitPrice**

No cálculo `const unitPrice = calcPrice(Math.max(1, quantity)) / Math.max(1, quantity);` o resultado já é em centavos por bilhete. Na linha `{fmt(unitPrice / 100)}` está dividindo por 100 duas vezes. Corrigir:

```tsx
// Linha correta (sem dividir por 100 duas vezes):
<span className="text-[#8B92A8]">{fmt(unitPrice)} por bilhete</span>
```

Onde `unitPrice` é em centavos, e `fmt` já converte de centavos para reais.

- [ ] **Step 3: Testar no browser**

```bash
npm run dev
```

Abrir `http://localhost:3000/rifa` e verificar:
- Contador de bilhetes visível
- Barra de progresso funciona
- Botões de atalho (1/3/5/10) preenchem o input
- Input manual atualiza o preço em tempo real
- Faixas de preço corretas: 1=R$25, 3=R$60, 5=R$90, 10=R$150

- [ ] **Step 4: Commit**

```bash
git add src/app/rifa/
git commit -m "feat(rifa): add public raffle page with quantity selector and price tiers"
```

---

## Task 11: Página de Confirmação

**Files:**
- Create: `src/app/rifa/confirmacao/[token]/page.tsx`

- [ ] **Step 1: Implementar**

```tsx
// src/app/rifa/confirmacao/[token]/page.tsx
import { notFound } from 'next/navigation';
import Link from 'next/link';

export const dynamic = 'force-dynamic';

interface ConfirmacaoData {
  buyer_name: string;
  ticket_quantity: number;
  amount_cents: number;
  ticket_numbers: number[];
  created_at: string;
}

export default async function ConfirmacaoPage({
  params,
}: {
  params: Promise<{ token: string }>;
}) {
  const { token } = await params;
  const baseUrl = process.env.NEXT_PUBLIC_SITE_URL ?? 'http://localhost:3000';
  const res = await fetch(`${baseUrl}/api/raffle/confirmacao/${token}`, { cache: 'no-store' });
  if (!res.ok) notFound();

  const data: ConfirmacaoData = await res.json();

  function fmt(cents: number) {
    return (cents / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });
  }

  return (
    <main className="flex min-h-screen items-center justify-center bg-[#0a0a0a] px-4 text-white">
      <div className="w-full max-w-lg">
        {/* Ícone de sucesso */}
        <div className="mb-8 flex justify-center">
          <div className="flex h-20 w-20 items-center justify-center rounded-full bg-green-500/20">
            <svg className="h-10 w-10 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
            </svg>
          </div>
        </div>

        <h1 className="mb-2 text-center text-3xl font-bold">Bilhetes confirmados!</h1>
        <p className="mb-8 text-center text-[#8B92A8]">
          Obrigado, {data.buyer_name}. Boa sorte!
        </p>

        {/* Números dos bilhetes */}
        <div className="mb-6 rounded-2xl bg-white/5 p-6">
          <p className="mb-4 text-sm text-[#8B92A8]">Seus bilhetes</p>
          <div className="flex flex-wrap gap-2">
            {data.ticket_numbers.map((n) => (
              <span
                key={n}
                className="inline-flex h-10 w-10 items-center justify-center rounded-full bg-[#FF4D2D] text-sm font-bold"
              >
                {n}
              </span>
            ))}
          </div>
        </div>

        {/* Resumo */}
        <div className="mb-8 rounded-2xl bg-white/5 p-6">
          <div className="flex justify-between text-sm">
            <span className="text-[#8B92A8]">Quantidade</span>
            <span>{data.ticket_quantity} bilhete{data.ticket_quantity > 1 ? 's' : ''}</span>
          </div>
          <div className="mt-3 flex justify-between text-sm">
            <span className="text-[#8B92A8]">Total pago</span>
            <span className="font-bold text-green-400">{fmt(data.amount_cents)}</span>
          </div>
        </div>

        <p className="mb-6 rounded-xl bg-yellow-500/10 px-4 py-3 text-center text-sm text-yellow-300">
          Guarde o link desta página — ele é a sua prova de compra.
        </p>

        <Link
          href="/rifa"
          className="block w-full rounded-xl bg-white/10 py-3 text-center text-sm font-medium transition hover:bg-white/20"
        >
          Voltar para a rifa
        </Link>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Testar**

Após uma compra real ou de teste no Stripe, acessar `/rifa/confirmacao/[token]` e verificar que os números dos bilhetes aparecem corretamente.

- [ ] **Step 3: Commit**

```bash
git add src/app/rifa/confirmacao/
git commit -m "feat(rifa): add purchase confirmation page"
```

---

## Task 12: Admin Page /admin/rifa

**Files:**
- Create: `src/app/admin/rifa/page.tsx`
- Modify: `src/components/admin/sidebar-nav.tsx`

- [ ] **Step 1: Admin page**

```tsx
// src/app/admin/rifa/page.tsx
import { adminContainer } from '@/infrastructure/di/container';

export const dynamic = 'force-dynamic';

export default async function AdminRifaPage() {
  const c = adminContainer();
  const raffle = await c.raffleRepo.findActive();

  if (!raffle) {
    return (
      <div>
        <h1 className="text-2xl font-bold text-[#1C1E2E]">Rifa</h1>
        <p className="mt-4 text-[#8B92A8]">Nenhuma rifa ativa no momento.</p>
      </div>
    );
  }

  const paid = await c.raffleTickets.countByStatus(raffle.id, 'paid');
  const reserved = await c.raffleTickets.countByStatus(raffle.id, 'reserved');
  const available = raffle.totalTickets - paid - reserved;
  const purchases = await c.rafflePurchases.listByRaffle(raffle.id);
  const paidPurchases = purchases.filter((p) => p.status === 'paid');
  const totalRevenue = paidPurchases.reduce((s, p) => s + p.amountCents, 0);

  function fmt(cents: number) {
    return (cents / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });
  }

  return (
    <div>
      <h1 className="text-2xl font-bold text-[#1C1E2E]">Rifa — {raffle.title}</h1>

      {/* Status cards */}
      <div className="mt-6 grid grid-cols-2 gap-4 sm:grid-cols-4">
        {[
          { label: 'Vendidos', value: paid, color: 'text-green-600' },
          { label: 'Reservados', value: reserved, color: 'text-yellow-600' },
          { label: 'Disponíveis', value: available, color: 'text-blue-600' },
          { label: 'Arrecadado', value: fmt(totalRevenue), color: 'text-[#FF4D2D]' },
        ].map((c) => (
          <div key={c.label} className="rounded-2xl bg-white p-4 shadow-sm">
            <p className="text-sm text-[#8B92A8]">{c.label}</p>
            <p className={`mt-1 text-2xl font-bold ${c.color}`}>{c.value}</p>
          </div>
        ))}
      </div>

      {/* Status da rifa */}
      <div className="mt-4 rounded-2xl bg-white p-4 shadow-sm">
        <span className="text-sm font-medium text-[#8B92A8]">Status: </span>
        <span className="font-bold capitalize">{raffle.status}</span>
        {raffle.drawDate && (
          <span className="ml-4 text-sm text-[#8B92A8]">
            Sorteio em {new Date(raffle.drawDate).toLocaleDateString('pt-BR')}
          </span>
        )}
        {raffle.winnerTicketId && (
          <span className="ml-4 text-sm font-bold text-green-600">
            Ganhador apurado ✓
          </span>
        )}
      </div>

      {/* Tabela de compradores */}
      <div className="mt-6 rounded-2xl bg-white shadow-sm">
        <div className="border-b border-[#E4EAF4] px-6 py-4">
          <h2 className="font-bold text-[#1C1E2E]">Compradores ({paidPurchases.length})</h2>
        </div>
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead className="bg-[#F5F7FF]">
              <tr>
                {['Nome', 'E-mail', 'Bilhetes', 'Valor', 'Data'].map((h) => (
                  <th key={h} className="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-[#8B92A8]">
                    {h}
                  </th>
                ))}
              </tr>
            </thead>
            <tbody className="divide-y divide-[#E4EAF4]">
              {paidPurchases.map((p) => (
                <tr key={p.id} className="hover:bg-[#F5F7FF]">
                  <td className="px-4 py-3 font-medium text-[#1C1E2E]">{p.buyerName}</td>
                  <td className="px-4 py-3 text-[#8B92A8]">{p.buyerEmail}</td>
                  <td className="px-4 py-3">{p.ticketQuantity}</td>
                  <td className="px-4 py-3 font-medium">{fmt(p.amountCents)}</td>
                  <td className="px-4 py-3 text-[#8B92A8]">
                    {new Date(p.createdAt).toLocaleDateString('pt-BR')}
                  </td>
                </tr>
              ))}
              {paidPurchases.length === 0 && (
                <tr>
                  <td colSpan={5} className="px-4 py-8 text-center text-[#8B92A8]">
                    Nenhuma venda ainda.
                  </td>
                </tr>
              )}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Adicionar item na sidebar**

Em `src/components/admin/sidebar-nav.tsx`, adicionar ao array `NAV` (após o item de Assinaturas):

```typescript
{ href: '/admin/rifa', label: 'Rifa', icon: Gift },
```

E importar `Gift` do `lucide-react`:

```typescript
import {
  LayoutDashboard, Package, ShoppingCart, Users,
  Mail, BookOpen, Ticket, Link2, Eye, CreditCard, Gift,
} from 'lucide-react';
```

- [ ] **Step 3: Testar**

Abrir `http://localhost:3000/admin/rifa` como admin e verificar que os dados da rifa aparecem.

- [ ] **Step 4: Commit**

```bash
git add src/app/admin/rifa/ src/components/admin/sidebar-nav.tsx
git commit -m "feat(rifa): add admin raffle page and sidebar nav item"
```

---

## Task 13: Verificação Final

- [ ] **Step 1: Build de produção sem erros**

```bash
npm run build
```

Verificar que não há erros de TypeScript nem de build.

- [ ] **Step 2: Fluxo completo de teste**

1. Abrir `/rifa` — confirmar que carrega sem erros
2. Selecionar 3 bilhetes → preço deve mostrar R$60,00
3. Preencher nome e e-mail → clicar "Comprar"
4. Deve redirecionar para Stripe Checkout
5. No Stripe, usar cartão de teste `4242 4242 4242 4242` (qualquer data futura, qualquer CVC)
6. Após pagamento → deve redirecionar para `/rifa/confirmacao/[token]`
7. Confirmar que os números dos bilhetes aparecem
8. Verificar no `/admin/rifa` que a compra aparece na tabela

- [ ] **Step 3: Verificar webhook**

No Stripe Dashboard → Developers → Webhooks, confirmar que o evento `checkout.session.completed` foi recebido e processado com status 200.

- [ ] **Step 4: Commit final se necessário**

```bash
git add -p
git commit -m "fix(rifa): final adjustments after integration testing"
```

---

## Notas de Deploy

Variáveis de ambiente necessárias (além das já existentes):
- `CRON_SECRET` — string aleatória longa para autenticar crons (ex: `openssl rand -hex 32`)

Após deploy:
- Verificar que as crons aparecem no Vercel Dashboard → Settings → Crons
- Disparar manualmente o cleanup: `GET /api/cron/raffle-cleanup` com header `Authorization: Bearer <CRON_SECRET>`
